/**
 * @brief Code behind for Player.qml
 *
 * Frontend properties used here:
 * @property [ListModel] clipList
 */
function recalculatePositions() {
    for(var i = 0; i < clipList.count;i++)
    {
        //Here comes the O(n^2) expensive positions calculation. But.. it's for academical purposes, so we don't care
    }
}
